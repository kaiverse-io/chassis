"""Smoke test — verifies the package is importable. Replace with real tests."""


def test_import() -> None:
    import {{ python_package_name }}  # noqa: F401  # smoke: package importable

    assert {{ python_package_name }} is not None
